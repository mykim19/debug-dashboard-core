"""LLM integration — LiteLLM-based multi-provider support.

Supports: Claude (Anthropic), GPT (OpenAI), Gemini (Google), Ollama (local).
LiteLLM is lazily imported — if not installed, Tier 1 still works.
"""
